language: ruby
cache: bundler

rvm:
  - 2.2.5

services:
  - postgresql

before_script:
  - psql -c 'create database tasty_brew_test;' -U postgres
  - bundle exec rake db:migrate
  - bundle exec rails s -d
